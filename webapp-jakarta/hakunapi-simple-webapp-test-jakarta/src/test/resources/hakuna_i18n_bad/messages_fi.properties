# A catalog with a key outside the localizable whitelist. Startup must fail
# naming this key rather than silently dropping it, and it must not be able to
# override unrelated configuration.
api.title=Otsikko
getfeatures.limit.max=999999
