# Finnish localization catalog for addresses.properties
#
# A sparse overlay: only keys that differ from the base configuration need to
# be present, and resolution is per key. Read as UTF-8.
#
# For collection titles and descriptions a schema file entry
# (addresses_schema_fi.json) is consulted when this catalog has no value, so a
# key here is only needed to override the schema, or where no schema exists.

api.title=INSPIRE-osoitteet, Suomi
api.description=Esimerkkitoteutus OGC API -rajapinnoista hakunapilla. INSPIRE Simple Addresses -aineisto Suomesta.

collections.addresses.title=Osoitteet
collections.addresses.description=Osoitepisteet koko Suomesta
collections.addresses_view.title=Osoitteet (näkymä)
