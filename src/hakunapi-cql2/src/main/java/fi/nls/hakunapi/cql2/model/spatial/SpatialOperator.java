package fi.nls.hakunapi.cql2.model.spatial;

public enum SpatialOperator {

    S_INTERSECTS,
    S_EQUALS,
    S_DISJOINT,
    S_TOUCHES,
    S_WITHIN,
    S_OVERLAPS,
    S_CROSSES,
    S_CONTAINS,
    ;

}
